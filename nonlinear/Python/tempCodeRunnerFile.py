  # fig, axes = plt.subplots(1, 4)

  # axes[0].plot(t[:999], x_arr, label='x_actual')
  # plt.legend()

  # axes[0].plot(t[:999], x_d_arr, label='x_des')
  # plt.legend()

  # axes[0].plot(t[:999], e_arr, label='e_arr')

  # axes[0].set_title('Actual output')
  # axes[0].set(ylabel='Amplitude', xlabel='Time (seconds)')
  # plt.legend()